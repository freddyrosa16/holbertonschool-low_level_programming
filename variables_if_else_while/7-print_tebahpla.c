#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - first function, prints the function
 * Return: return value 0
 */
int main(void)
{
	char l;

	for (l = 'z' ; l >= 'a' ; l--)
	{putchar(l); }
	putchar('\n');
	return (0);
}
