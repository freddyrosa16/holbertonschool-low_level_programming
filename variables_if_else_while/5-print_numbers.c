#include <stdio.h>
/**
 * main - first function, and prints the function
 * Return: return value 0
 */
int main(void)
{
	char n;

	for (n = '0' ; n <= '10' ; n++)
	{
		putchar (n);
	}
	putchar('\n');
	return (0)
}
