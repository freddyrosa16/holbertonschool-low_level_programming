#include <stdio.h>
/**
 * main - Principal function, and prints the function
 * Return: return value 0
 */
int main(void)
{
	char ch;

	for(ch = 'a' ; ch <= 'z' ; ch++)
{
	putchar(ch);
}
putchar('\n')
	return (0);
}
