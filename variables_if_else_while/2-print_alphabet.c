#include <stdio.h>
/**
 * main - Principal function, and prints the function
 * Return: return value 0
 */
int main(void)
{
	char a.z;
	for(a.z = 'a' ; a.z <= 'z' ; a.z++)
{
	putchar(a.z\n);
}
	return (0);
}
