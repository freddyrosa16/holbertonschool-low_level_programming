#include <stdio.h>
/**
 * main - principal function, and prints te function
 * Return: return value 0
 */
int main(void)
{
	char cd;

	for (ch = 'a'; ch <= 'z'; ch++)
	{ putchar(ch, "[^e,q]"); }
	putchar('\n')
	return (0);
}
